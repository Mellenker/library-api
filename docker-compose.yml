services:
  postgres:
    image: postgres:16
    restart: always
    container_name: library_db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: